# سياسة تطوير المشروع ومزامنة GitHub

## الدفع المباشر إلى الفرع الرئيسي

يجب دفع كل تعديل مكتمل على مشروع **ديوني وميزانيتي برو** مباشرةً إلى `github/main`. بعد إتمام أي تعديل في الشيفرة أو التوثيق أو الإعدادات، شغّل الاختبارات الملائمة، وأنشئ التزاماً على الفرع `main`. يتولى الخطاف `.githooks/post-commit` دفع الالتزام تلقائياً إلى GitHub؛ بعد ذلك تحقق من تطابق رأس `github/main` مع الالتزام المحلي.

لا تترك تعديلات مكتملة غير ملتزم بها، ولا تعطل خطاف الدفع التلقائي. إذا فشل الدفع بسبب الشبكة أو المصادقة، يبقى الالتزام آمناً محلياً، ويجب إعادة الدفع إلى `github/main` فور عودة الاتصال أو الصلاحية.

## تفعيل بيئة عمل جديدة

ملفات الخطاف والسياسة محفوظة في المستودع، لكن إعداد `core.hooksPath` خاص بكل نسخة عمل في Git. قبل أول تعديل في نسخة مستنسخة أو بيئة جديدة، نفّذ:

```bash
node scripts/install-github-autosync.mjs
```

ثم تحقق من أن `git config --local --get core.hooksPath` يعرض `.githooks`.
