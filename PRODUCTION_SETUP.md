# إعداد الإنتاج لتطبيق ديوني وميزانيتي برو

## النشر الدائم

يُنشر الموقع من واجهة إدارة المشروع عبر زر **Publish** بعد إنشاء نقطة إصدار. بعد ظهور النطاق النهائي، أضِفه إلى قائمة **Authorized domains** في Firebase Authentication. من دون هذه الخطوة قد يفشل الدخول بالبريد أو Google عند استخدام النطاق الدائم.

## Firebase Authentication وFirestore

يعتمد التطبيق على Firebase Authentication وFirestore لعزل البيانات المالية لكل مستخدم. قبل إطلاق الموقع تأكد من تمكين طريقتي الدخول اللتين تستخدمهما الواجهة:

| الإعداد | القيمة المطلوبة |
| --- | --- |
| Firebase Authentication | Email/Password وGoogle مفعّلان |
| Authorized domains | نطاق الموقع المنشور وأي نطاق مخصص |
| Cloud Firestore Rules | نشر محتوى ملف `firestore.rules` المرفق |
| Firestore Database | قاعدة البيانات المشار إليها في `client/firebase-applet-config.json` متاحة للمستخدمين المصرح لهم فقط |

تطبّق القواعد المرفقة مبدأ الرفض الافتراضي، وتسمح بالقراءة والكتابة لمالك البيانات أو للمساعد المسجل له فقط، كما تتحقق من هوية السجل وحدود النصوص والقيم غير السالبة.

## النسخ الاحتياطي عبر Google Drive

النسخ المحلي بصيغة JSON يعمل داخل المتصفح. أما النسخ عبر Google Drive فيستخدم OAuth الخاص بالمستخدم ونطاق `https://www.googleapis.com/auth/drive.file`، ولا يحتاج إلى حفظ رمز وصول في الخادم. قبل تفعيله للمستخدمين:

1. اضبط شاشة موافقة OAuth في Google Cloud واجعل التطبيق Production عند اكتمال الاختبارات.
2. أضف النطاق المنشور إلى مصادر JavaScript المصرح بها وإلى نطاقات إعادة التوجيه المصرح بها عند الحاجة.
3. فعّل Google Drive API للمشروع نفسه.
4. راجع قائمة نطاقات OAuth، وأبقِ نطاق `drive.file` فقط لأن التطبيق يتعامل مع الملفات التي ينشئها هو.

> لا يُنصح بنشر الموقع قبل تأكيد قواعد Firestore والنطاقات المصرح بها، لأنهما يحكمان الوصول إلى البيانات المالية وتسجيل الدخول.
