# خط أساس أداء الإنتاج — 23 أغسطس 2026

## القياس

تمت مراجعة `https://www.iqcma.com/` بعد تسجيل الدخول. زمن اكتمال وثيقة التنقل الذي أظهره المتصفح كان نحو **1,512ms**، إلا أن شاشة البداية تحمل عدة حزم JavaScript كبيرة دفعة واحدة.

| المورد | الحجم المفكوك التقريبي | مدة الطلب المرصودة |
|---|---:|---:|
| حزمة Firebase | 772,143 بايت | 420ms |
| حزمة التطبيق الرئيسية | 756,524 بايت | 418ms |
| حزمة PDF | 593,971 بايت | 418ms |
| حزمة الرسوم البيانية | 443,045 بايت | 414ms |
| حزمة الحركة | 127,459 بايت | 406ms |
| CSS الرئيسي | 134,014 بايت | 398ms |

## الاستنتاج المبدئي

التحميل الأولي يجلب وحدات PDF والرسوم البيانية والحركة مع لوحة التحكم، رغم أن هذه الأدوات ليست مطلوبة في أول عرض. هذا هو المرشح الرئيسي للثقل، وتستهدف التحسينات فصل الصفحات والأدوات المكلفة إلى تحميل كسول مع تجنب استيرادها في مسار لوحة التحكم الأول.

## نتيجة التحسين قبل النشر

تم فصل الرسوم والتحليلات خلف زر «تحميل التحليلات»، وتحويل صفحات الديون والميزانية والتنبيهات ومعاينة التطوير إلى تحميل كسول. انخفضت حزمة التطبيق الرئيسية في بناء الإنتاج من **1,246.61KB** إلى **804.94KB**، أي انخفاض محسوب قدره **35.00%** قبل الضغط. اجتازت النسخة 109 اختبارات وفحص TypeScript وبناء الإنتاج.

يبقى القياس الشبكي النهائي مشروطاً بنشر هذه النسخة ثم إعادة فحص `iqcma.com`، لأن نطاق الإنتاج ما زال يخدم الإصدار السابق حتى يتم النشر.

## تحسين الأصول الثابتة وHTML الإنتاجي

لم توجد صور محتوى أو ملفات وسائط كبيرة داخل الأصول المحلية للتطبيق كي تتطلب إعادة ضغط. كانت الأصول الثابتة المحلية الفعلية تقتصر على ملف جمع سجلات التطوير (`debug-collector.js`) بحجم 26,759 بايت، ولذلك أُبقي للاستخدام المحلي فقط ولم يعد يُنسخ إلى بناء الإنتاج.

كما كان وقت تشغيل أدوات التطوير يُحقن داخل `index.html` الإنتاجي، فبلغ حجمه 368,858 بايت. جرى جعله خاصاً بخادم التطوير، فأصبح ملف HTML المنتج 1,636 بايت؛ وهو انخفاض محسوب قدره 99.00% في ملف HTML. لا يؤثر ذلك في واجهة التطبيق أو البيانات، ويظل وقت التشغيل وجمع السجلات متاحين في المعاينة المحلية.

تم أيضاً تحديث خادم التطبيق لحل إعداد Vite الديناميكي في وضع التطوير قبل إنشاء وسيط Vite. بذلك بقيت معاينة التطوير وFast Refresh تعملان، بينما يستخدم بناء الإنتاج إضافات نظيفة فقط. التحقق النهائي نجح بـ 111 اختباراً وفحص TypeScript وبناء إنتاج يحتوي على HTML بحجم 1,636 بايت ولا ينسخ ملف `debug-collector.js` إلى النشر.
