# التحقق من أرقام الديون والفواتير المرجعية

## الصيغة

| السجل | الرقم الجديد | الرقم المعروض للسجل السابق |
| --- | --- | --- |
| دين | `DBT-YYYY-NNNN` | `DBT-ARCH-<آخر 6 رموز من المعرّف>` |
| فاتورة / مصروف | `INV-YYYY-NNNN` | `INV-ARCH-<آخر 6 رموز من المعرّف>` |

يُخزَّن الرقم المرجعي الجديد في حقل `referenceNumber` عند الإنشاء. أمّا السجلات السابقة فلا يجري تعديلها أو تغيير معرّفاتها؛ بل يُشتق لها مرجع عرض ثابت من المعرّف الموجود عند الحاجة.

## التحقق

- اجتازت اختبارات الوحدة ستة اختبارات، منها تسلسل الديون، وتسلسل الفواتير المستقل، ومرجع العرض الثابت لكل من الدين والفاتورة القديمة غير المرقمة.
- اجتاز المشروع فحص TypeScript والبناء الإنتاجي بعد إضافة الحقول والعروض.
- استُخدم حساب اختبار معزول في المعاينة، وتظهر به حركتا دين منفصلتان بقيمتي 500 ر.س و175 ر.س. ستستقبل أي إضافة دين لاحقة الرقم `DBT-2026-0001` ما لم توجد أرقام مخزنة سابقة لسنة 2026 في ذلك الحساب.
- تأكدت المعاينة بعد اكتمال التحميل من وصول حساب الاختبار نفسه وظهور إجمالي الديون 675 ر.س، مع توفر تبويب **الديون والالتزامات** و**الميزانية والمصاريف** للتحقق الوظيفي للترقيم.
- في كشف حساب عميل الاختبار ظهرت المرجعات المتوافقة للسجلين السابقين كـ `د#DBT-ARCH-41DQIN` و`د#DBT-ARCH-N4WESJ`، كما تضمّن رابط مشاركة واتساب هذين المرجعين في نص الكشف.
- **تحقق وظيفي ناجح:** أُضيف دين اختبار بقيمة 120 ر.س إلى الحساب القائم، فظهر فوراً كبند مستقل في كشف الحساب تحت المرجع `د#DBT-2026-0001`. كما ظهر المرجع نفسه في رابط مشاركة واتساب، وأصبح إجمالي الحساب 795 ر.س عبر ثلاثة بنود مستقلة.
- فُتح نموذج تسجيل المصروف في شاشة الميزانية، ويعرض الحقول اللازمة لإدخال مبلغ المصروف وفئته وتاريخه وبيانه، تمهيداً لاختبار مرجع الفاتورة `INV` عبر الواجهة نفسها.
- **تحقق وظيفي ناجح:** سُجّل مصروف اختبار بقيمة 25 ر.س، فظهر في جدول سجل المصروفات تحت مرجع الفاتورة `ف#INV-2026-0001`، مؤكداً استقلال تسلسل الفواتير عن تسلسل الديون وظهوره في واجهة الميزانية.
- أُعيد تشغيل فحوص الإصدار بعد التحقق الوظيفي: نجحت اختبارات Vitest الستة في أربع مجموعات، ونجح فحص TypeScript والبناء الإنتاجي. ظهر تحذير غير مانع فقط بخصوص حجم بعض الحزم بعد التصغير.
- لبدء التحقق من البحث بالرقم المرجعي، حُمّلت جلسة حساب الاختبار بنجاح. تحتوي البيانات الحالية على ثلاثة ديون بإجمالي 795 ر.س ومصروف واحد بقيمة 25 ر.س؛ ما يوفّر مرجعي الاختبار `DBT-2026-0001` و`INV-2026-0001`.
- **تحقق بصري ناجح للديون:** أدّى إدخال صيغة العرض العربية `د#DBT 2026 0001` في حقل البحث إلى تصفية سجل الديون من ثلاثة بنود إلى بند واحد فقط، وهو دين الاختبار تحت المرجع `د#DBT-2026-0001`. يثبت ذلك تجاهل حالة الأحرف والفواصل ودعم بادئة العرض العربية.
- **تحقق بصري للفواتير:** قَبِل حقل بحث المصروفات صيغة العرض العربية المختلطة `ف#inv 2026 0001` وأبقى السجل المطابق `ف#INV-2026-0001` ظاهراً. ستُستكمل مطابقة حالة عدم وجود نتائج للتأكد من أن الفلترة لا تعتمد على وجود بند واحد فقط.
- **تحقق بصري لحالة عدم التطابق:** أدّى البحث عن `INV-2026-9999` إلى إظهار «عدد العمليات: 0» والحالة الفارغة بدلاً من مصروف الاختبار. وبذلك تأكد أن فلترة الفواتير تعمل فعلياً، لا أنها نتيجة وجود مصروف واحد فقط.
- توسعت اختبارات Vitest إلى ثماني حالات ناجحة في أربع مجموعات، وتشمل مطابقة مرجع الدين مع تجاهل حالة الأحرف والفواصل وبادئة العرض العربية، ومطابقة المرجع الأرشيفي للفواتير.

## مواضع العرض

تظهر مراجع الديون في بطاقة الدين، كشف الحساب، كشف واتساب، والطباعة. وتظهر مراجع الفواتير في جدول الميزانية والمصروفات. تظل الأرقام قابلة للبحث والنسخ من الواجهة.
